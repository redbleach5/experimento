# 📦 Подробная инструкция по установке

## Системные требования

- **ОС**: Windows 10/11, Linux, macOS
- **GPU**: NVIDIA RTX 3090 (24GB VRAM) или аналогичная
- **RAM**: 32GB (рекомендуется)
- **Python**: 3.9 или выше
- **CUDA**: 11.8+ (для GPU ускорения)
- **Свободное место**: ~10GB для моделей и зависимостей

## Пошаговая установка

### 1. Установка Python

#### Windows:
1. Скачайте Python 3.9+ с https://www.python.org/downloads/
2. При установке отметьте "Add Python to PATH"
3. Проверьте установку:
   ```bash
   python --version
   ```

#### Linux:
```bash
sudo apt update
sudo apt install python3 python3-pip
```

#### macOS:
```bash
brew install python3
```

### 2. Установка CUDA (для GPU)

#### Windows:
1. Скачайте CUDA Toolkit: https://developer.nvidia.com/cuda-downloads
2. Установите CUDA 11.8 или выше
3. Проверьте установку:
   ```bash
   nvidia-smi
   ```

#### Linux:
```bash
# Следуйте инструкциям NVIDIA для вашего дистрибутива
# https://developer.nvidia.com/cuda-downloads
```

### 3. Установка Ollama

#### Windows:
1. Скачайте установщик: https://ollama.ai/download/windows
2. Запустите установщик
3. Ollama должен запуститься автоматически

#### Linux:
```bash
curl -fsSL https://ollama.ai/install.sh | sh
```

#### macOS:
```bash
brew install ollama
```

Проверьте установку:
```bash
ollama --version
```

Запустите Ollama (если не запущен):
```bash
ollama serve
```

### 4. Установка Python зависимостей

Перейдите в директорию проекта и выполните:

```bash
pip install -r requirements.txt
```

Или используйте виртуальное окружение (рекомендуется):

```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 5. Скачивание модели

Для RTX 3090 рекомендуются следующие модели:

#### Рекомендуемая (лучший баланс):
```bash
ollama pull deepseek-coder:6.7b
```

#### Альтернативы:
```bash
# Больше параметров, лучше качество
ollama pull codellama:13b

# Современная архитектура
ollama pull qwen2.5-coder:7b

# Специализированная для кода
ollama pull starcoder2:7b
```

Проверьте установленные модели:
```bash
ollama list
```

### 6. Настройка конфигурации

Откройте `config.yaml` и настройте:

```yaml
model:
  model_name: "deepseek-coder:6.7b"  # Ваша модель
```

### 7. Проверка установки

Запустите скрипт проверки:

```bash
python setup.py
```

Или вручную проверьте:

```bash
# Проверка Python
python --version

# Проверка Ollama
ollama list

# Проверка GPU
nvidia-smi

# Проверка зависимостей
python -c "import torch; print(torch.__version__)"
```

## Запуск

### CLI интерфейс:
```bash
python cli.py
```

### Веб-интерфейс:
```bash
python web_ui.py
```
Откройте браузер: http://127.0.0.1:8000

### Через скрипт:
- Windows: `run.bat`
- Linux/Mac: `bash run.sh` или `chmod +x run.sh && ./run.sh`

## Устранение проблем

### Ollama не запускается

**Windows:**
- Проверьте, что Ollama запущен (иконка в трее)
- Перезапустите Ollama из меню Пуск

**Linux/Mac:**
```bash
ollama serve
```

### Модель не найдена

```bash
# Проверьте список моделей
ollama list

# Скачайте модель
ollama pull deepseek-coder:6.7b
```

### Ошибки с CUDA

1. Убедитесь, что CUDA установлена:
   ```bash
   nvidia-smi
   ```

2. Переустановите PyTorch с поддержкой CUDA:
   ```bash
   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
   ```

### Недостаточно памяти GPU

- Используйте меньшую модель (6.7b вместо 13b)
- Включите квантование в `config.yaml`:
  ```yaml
  gpu:
    use_8bit: true
  ```

### Медленная генерация

1. Проверьте использование GPU:
   ```bash
   nvidia-smi
   ```

2. Убедитесь, что модель использует GPU (в логах Ollama)

3. Используйте более легкую модель

## Дополнительная настройка

### Оптимизация для RTX 3090

В `config.yaml`:

```yaml
gpu:
  use_gpu: true
  max_memory: 24
  use_flash_attention: true
  use_8bit: false  # Не нужно для 6.7b моделей
```

### Изменение системного промпта

Отредактируйте `system_prompt` в `config.yaml` для изменения поведения агента.

## Готово!

Теперь вы можете использовать AI Code Agent для помощи в программировании! 🎉

