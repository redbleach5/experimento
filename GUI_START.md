# 🎨 Запуск GUI интерфейса

## Быстрый старт

### 1. Установите Ollama или LM Studio

**Вариант 1: Ollama (рекомендуется для CLI)**
- Через скрипт: `powershell -ExecutionPolicy Bypass -File install_ollama.ps1`
- Или вручную: https://ollama.ai/download/windows
- Или через winget: `winget install Ollama.Ollama`

**Вариант 2: LM Studio (рекомендуется для GUI)**
- Скачайте с https://lmstudio.ai
- Установите и запустите
- Включите Local Server в настройках (порт 1234)
- См. подробную инструкцию: `LM_STUDIO_SETUP.md`

### 2. Установите модель

**Для Ollama:**
```bash
setup_model.bat
# или
ollama pull deepseek-coder:6.7b
```

**Для LM Studio:**
1. Откройте LM Studio
2. Перейдите в "Search"
3. Найдите и скачайте модель (например, "deepseek-coder")
4. Загрузите модель в разделе "Chat"

### 3. Запустите GUI

**Простой способ:**
```bash
start_gui.bat
```

**Или напрямую:**
```bash
python gui.py
```

## Возможности GUI

### ✨ Основные функции

- **Современный интерфейс** - Темная тема, удобная навигация
- **Поддержка провайдеров** - Работает с Ollama и LM Studio
- **Автоматическая проверка** - Проверяет провайдер и модели при запуске
- **Установка моделей** - Установите модели прямо из интерфейса (Ollama)
- **История диалогов** - Все разговоры сохраняются автоматически
- **Сохранение чатов** - Экспортируйте историю в JSON

### 🎯 Использование

1. **Отправка запросов:**
   - Введите ваш вопрос в нижнее поле
   - Нажмите "Отправить" или Ctrl+Enter
   - Дождитесь ответа (генерируется в реальном времени)

2. **Выбор провайдера:**
   - Выберите "ollama" или "lmstudio" из выпадающего списка
   - Система автоматически переключится на выбранный провайдер

3. **Выбор модели:**
   - Выберите модель из выпадающего списка в боковой панели
   - Нажмите "Обновить список моделей" для проверки новых моделей

4. **Установка модели (только для Ollama):**
   - Выберите модель из списка
   - Нажмите "Установить модель"
   - Дождитесь завершения установки

4. **Управление историей:**
   - "Очистить чат" - удаляет текущую историю
   - "Сохранить историю" - экспортирует в JSON файл

## Примеры запросов

```
Напиши функцию на Python для быстрой сортировки массива
```

```
Создай REST API на FastAPI с JWT аутентификацией и CRUD операциями
```

```
Объясни разницу между async/await и Promise в JavaScript
```

```
Напиши класс для работы с базой данных SQLite на Python
```

## Устранение проблем

### GUI не запускается

1. Проверьте Python:
   ```bash
   python --version
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Проверьте tkinter:
   ```bash
   python -c "import tkinter; print('OK')"
   ```

### Провайдер не найден

**Для Ollama:**
1. Убедитесь, что Ollama установлен:
   ```bash
   ollama --version
   ```
2. Если не установлен, запустите:
   ```bash
   install_ollama.ps1
   ```

**Для LM Studio:**
1. Убедитесь, что LM Studio запущен
2. Проверьте, что Local Server включен (порт 1234)
3. Убедитесь, что модель загружена в разделе "Chat"
4. См. подробную инструкцию: `LM_STUDIO_SETUP.md`

### Модель не найдена

1. Проверьте установленные модели:
   ```bash
   ollama list
   ```

2. Установите модель:
   ```bash
   ollama pull deepseek-coder:6.7b
   ```

3. Или используйте кнопку "Установить модель" в GUI

### Медленная генерация

1. Проверьте использование GPU:
   ```bash
   nvidia-smi
   ```

2. Убедитесь, что Ollama использует GPU (должно быть видно в nvidia-smi)

3. Используйте более легкую модель (6.7b вместо 13b)

## Горячие клавиши

- **Ctrl+Enter** - Отправить сообщение
- **Enter** - Новая строка в поле ввода

## Рекомендации для RTX 3090

### Оптимальные модели:

1. **deepseek-coder:6.7b** ⭐ (рекомендуется)
   - Отличный баланс скорости и качества
   - ~4 GB памяти
   - Быстрая генерация

2. **codellama:13b**
   - Лучше качество кода
   - ~7 GB памяти
   - Медленнее, но точнее

3. **qwen2.5-coder:7b**
   - Современная архитектура
   - ~4.5 GB памяти
   - Хорошая поддержка русского языка

---

**Наслаждайтесь программированием с AI! 🚀**

