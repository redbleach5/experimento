# 📋 Инструкции для настройки LM Studio API

## ⚠️ Проблема
API возвращает 502, хотя модель работает в Chat. Это означает, что **Local Server не настроен правильно**.

## ✅ Что нужно сделать:

### Шаг 1: Откройте настройки Local Server
1. В LM Studio нажмите **Settings** (Настройки)
2. Выберите **Local Server** (Локальный сервер)

### Шаг 2: Проверьте настройки
Убедитесь что:
- ✅ Кнопка **"Start Server"** нажата (должно быть "Server running")
- ✅ Порт указан (обычно **1234**)
- ❌ **ОТКЛЮЧИТЕ** "Load model on demand" (Загрузка модели по требованию)
- ❌ **ОТКЛЮЧИТЕ** "Automatic unloading of unused models" (Автоматическая разгрузка)

### Шаг 3: Перезапустите Local Server
1. Нажмите **"Stop Server"**
2. Подождите **5 секунд**
3. Нажмите **"Start Server"**
4. Дождитесь статуса **"READY"** для модели

### Шаг 4: Проверьте статус модели
- В разделе **Local Server** модель должна быть видна как **"READY"**
- В **Chat** модель должна отвечать (это уже работает ✅)

### Шаг 5: Запустите тест
После настройки запустите:
```bash
python wait_and_test_api.py
```

Этот скрипт будет ждать готовности API и автоматически протестирует подключение.

## 🔍 Альтернатива: Если ничего не помогает

1. **Отправьте сообщение в Chat** в LM Studio
2. Дождитесь ответа модели
3. Это "разбудит" модель для API
4. Попробуйте снова

## 📝 Проверка логов

Если проблема продолжается:
1. Откройте **View → Developer Logs** в LM Studio
2. Ищите ошибки или предупреждения
3. Проверьте сообщения о загрузке модели

## 🎯 Цель

После настройки API должен отвечать **200 OK** вместо **502 Bad Gateway**.

