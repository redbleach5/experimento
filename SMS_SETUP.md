# 📱 Настройка отправки SMS

## Инструмент добавлен!

Теперь агент может отправлять SMS через инструмент `send_sms`.

## 🔧 Настройка через Twilio (рекомендуется)

### Шаг 1: Регистрация в Twilio
1. Зайдите на https://www.twilio.com
2. Создайте аккаунт (есть бесплатный пробный период)
3. Получите:
   - Account SID
   - Auth Token
   - Phone Number (номер для отправки)

### Шаг 2: Установка библиотеки
```bash
pip install twilio
```

### Шаг 3: Настройка переменных окружения
Создайте файл `.env` в корне проекта:
```
TWILIO_ACCOUNT_SID=your_account_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890
```

Или установите в системе:
```bash
# Windows PowerShell
$env:TWILIO_ACCOUNT_SID="your_sid"
$env:TWILIO_AUTH_TOKEN="your_token"
$env:TWILIO_PHONE_NUMBER="+1234567890"
```

## 📝 Использование

### Через агента:
```
Пользователь: "Отправь SMS на +797734030123 с текстом 'Привет!'"
Агент: Использует инструмент send_sms и отправляет сообщение
```

### Прямой вызов:
```python
from mcp_tools import MCPToolManager

tm = MCPToolManager()
result = tm.execute_tool(
    "send_sms",
    phone="+797734030123",
    message="Привет от AI агента!"
)
```

## ⚠️ Важно

- Twilio имеет ограничения на бесплатном тарифе
- Проверьте стоимость SMS для вашего региона
- Номер должен быть в международном формате (+7...)

## 🔄 Альтернативные сервисы

Можно добавить поддержку других сервисов:
- SMS.ru
- SMSC.ru
- Другие API

## ✅ Готово!

После настройки агент сможет отправлять SMS автоматически!

