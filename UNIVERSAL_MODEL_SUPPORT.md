# 🌟 Универсальная поддержка моделей

## ✅ Наше ПО работает с ЛЮБОЙ моделью из LM Studio!

### Как это работает:

1. **Автоматическое определение моделей:**
   - При запуске агент автоматически получает список всех доступных моделей из LM Studio
   - Показывает все найденные модели
   - Может использовать любую из них

2. **Умный выбор модели:**
   - Если модель из `config.yaml` найдена - использует её
   - Если не найдена - ищет похожую (частичное совпадение имени)
   - Если похожей нет - использует первую доступную
   - Если API не отвечает - использует модель из конфига

3. **Поддержка любых моделей:**
   - ✅ Qwen (любая версия)
   - ✅ Llama (любая версия)
   - ✅ Mistral
   - ✅ Phi
   - ✅ Flux (если в формате GGUF)
   - ✅ Любая другая модель в формате GGUF

## 🚀 Как использовать любую модель:

### Вариант 1: Автоматический выбор
1. Загрузите модель в LM Studio
2. Включите Local Server API
3. Запустите агента - он автоматически найдет и использует модель

### Вариант 2: Указать конкретную модель
1. Откройте `config.yaml`
2. Укажите имя модели:
   ```yaml
   model:
     model_name: "ваша-модель-здесь"
   ```
3. Запустите агента

### Вариант 3: Через GUI
1. Запустите `python gui.py`
2. Выберите провайдер "lmstudio"
3. Нажмите "Обновить список моделей"
4. Выберите нужную модель из списка

## 📋 Что нужно для работы:

1. **Модель в формате GGUF:**
   - LM Studio работает только с моделями в формате `.gguf`
   - Загрузите модель в LM Studio (Models -> Add Model)

2. **Local Server API включен:**
   - Settings -> Local Server API
   - Включите "Enable Local Server API"
   - Или нажмите "Start Server"

3. **Модель загружена:**
   - Модель должна быть видна в списке моделей LM Studio
   - Статус должен быть "READY" (не "Loading")

## 🧪 Тестирование:

Запустите тест для проверки всех моделей:
```bash
python test_any_model.py
```

Этот скрипт:
- Покажет все доступные модели
- Протестирует каждую модель
- Покажет какие работают, какие нет
- Протестирует работу через агента

## 💡 Примеры моделей:

### Для кодирования:
- `codellama:7b` - быстрая
- `codellama:13b` - лучше качество
- `deepseek-coder:6.7b` - отличная для кода
- `qwen2.5-coder:7b` - современная

### Для общего использования:
- `llama3:8b` - универсальная
- `mistral:7b` - хорошая для диалогов
- `qwen3-30b-a3b-instruct-2507` - мощная (как у вас)

### Для экспериментов:
- `flux-1.0-Q4_K_M` - если есть в GGUF
- `phi-4` - компактная и быстрая
- Любая другая модель в GGUF

## 🔧 Настройка:

### В config.yaml:
```yaml
model:
  provider: "lmstudio"
  model_name: "любая-модель-из-lm-studio"  # Или оставьте пустым для авто-выбора
```

### Параметры генерации:
```yaml
generation:
  max_tokens: 4096
  temperature: 0.2  # Низкая для кода, выше для творчества
  top_p: 0.95
```

## ✅ Итог:

**Наше ПО полностью универсально!**

- Работает с любой моделью из LM Studio
- Автоматически находит и использует модели
- Поддерживает любые форматы моделей (GGUF)
- Не требует специальной настройки для каждой модели

Просто загрузите модель в LM Studio, включите API, и всё заработает! 🚀

