# 🤖 AI Code Agent

Мощный AI-агент для помощи в написании кода, работающий на локальных моделях. Оптимизирован для RTX 3090 (24GB VRAM).

## ✨ Возможности

- 🚀 **Локальные модели** - Работает полностью офлайн, используя ваше железо
- 💻 **Специализация на коде** - Оптимизирован для помощи в программировании
- 🌐 **Веб-интерфейс** - Современный веб-интерфейс с поддержкой IDE
- 🔌 **Множество провайдеров** - Поддержка Ollama, LM Studio, OpenAI, Anthropic и любых OpenAI-совместимых API
- 🌐 **Универсальная поддержка моделей** - Автоматическое определение возможностей любых моделей через API
- ⚡ **Быстрая генерация** - Использует GPU для ускорения
- 📝 **История диалогов** - Сохранение всех разговоров
- 🔧 **Гибкая настройка** - Полная кастомизация через config.yaml
- 📂 **Автоматический контекст проекта** - Модели автоматически видят структуру проекта, README, конфиги и основные файлы
- 🔍 **Умный поиск файлов** - Автоматически находит релевантные файлы для ваших запросов
- 🛠️ **MCP инструменты** - Расширенные возможности для работы с файлами и проектом
- 🎯 **Адаптивная система** - Автоматически определяет возможности модели и оптимизирует параметры под неё
- ⚡ **Умное управление контекстом** - Автоматически ограничивает и оптимизирует контекст под размер модели

## 🛠️ Требования

- **GPU**: NVIDIA RTX 3090 (24GB VRAM) или аналогичная
- **RAM**: 32GB (как у вас)
- **Python**: 3.9+
- **CUDA**: 11.8+ (для GPU ускорения)
- **Ollama** или **LM Studio**: Для работы с локальными моделями

## 📦 Установка

### 1. Установите провайдер моделей

**Вариант 1: Ollama (рекомендуется для локальных моделей)**
- Windows: https://ollama.ai/download/windows
- Linux/Mac: https://ollama.ai/download

**Вариант 2: LM Studio (для локальных моделей)**
- Скачайте с https://lmstudio.ai
- Включите Local Server в настройках (порт 1234)

**Вариант 3: OpenAI API (облачные модели)**
- Получите API ключ на https://platform.openai.com/api-keys
- Установите переменную окружения: `export OPENAI_API_KEY=sk-...`

**Вариант 4: Anthropic (Claude)**
- Получите API ключ на https://console.anthropic.com/
- Установите переменную окружения: `export ANTHROPIC_API_KEY=sk-ant-...`

**Вариант 5: Любой OpenAI-совместимый API**
- Укажите `base_url` и `api_key` в конфигурации

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

### 3. Скачайте модель для кодирования

**Для Ollama:**
```bash
# Быстрая и эффективная (рекомендуется)
ollama pull deepseek-coder:6.7b

# Альтернативы:
ollama pull codellama:13b
ollama pull qwen2.5-coder:7b
ollama pull starcoder2:7b
```

**Для LM Studio:**
1. Откройте LM Studio
2. Перейдите в раздел "Search" 
3. Найдите и скачайте модель (например, "deepseek-coder" или "CodeLlama")
4. Загрузите модель в разделе "Chat"
5. Включите "Local Server" в настройках

### 4. Настройте конфигурацию

Отредактируйте `config.yaml` под свои нужды:

```yaml
model:
  provider: "ollama"  # или "lmstudio"
  model_name: "deepseek-coder:6.7b"  # Ваша модель
```

**Для LM Studio:**
```yaml
model:
  provider: "lmstudio"
  model_name: "deepseek-coder"  # Имя модели из LM Studio
```

**Для OpenAI:**
```yaml
model:
  provider: "openai"
  model_name: "gpt-4"  # или gpt-3.5-turbo, gpt-4-turbo и т.д.
openai:
  base_url: https://api.openai.com/v1
  api_key: sk-...  # Или используйте переменную окружения OPENAI_API_KEY
```

**Для Anthropic (Claude):**
```yaml
model:
  provider: "anthropic"
  model_name: "claude-3-opus-20240229"  # или claude-3-sonnet, claude-3-haiku
anthropic:
  base_url: https://api.anthropic.com/v1
  api_key: sk-ant-...  # Или используйте переменную окружения ANTHROPIC_API_KEY
```

**Для кастомного OpenAI-совместимого API:**
```yaml
model:
  provider: "custom"  # или любое другое имя
  model_name: "your-model-name"
custom:
  base_url: https://your-api-provider.com/v1
  api_key: your-api-key  # Или используйте переменную окружения
```

**Важно:** Система автоматически определяет возможности любой модели через API. Если модель не найдена в базе данных, она будет автоматически определена по имени или через API провайдера.

## 🚀 Использование

### 🌐 Веб-интерфейс

Запустите веб-сервер:

```bash
python web_ui.py
```

Затем откройте браузер: http://127.0.0.1:8000

Веб-интерфейс включает:
- ✅ Современный дизайн
- ✅ Режим чата для общения с AI
- ✅ IDE режим с файловым браузером и редактором кода
- ✅ WebSocket для потоковой передачи ответов
- ✅ История диалогов
- ✅ Автоматическое сохранение сессий

### Запуск веб-интерфейса с кастомными параметрами

```bash
python web_ui.py 0.0.0.0 8080  # Доступ из сети на порту 8080
```

## 📋 Примеры использования

### Пример 1: Создание функции

**Запрос:**
```
Напиши функцию на Python для быстрой сортировки массива с комментариями
```

**Ответ:** Агент предоставит полный код с комментариями

### Пример 2: Объяснение концепции

**Запрос:**
```
Объясни разницу между async/await и Promise в JavaScript
```

### Пример 3: Создание проекта

**Запрос:**
```
Создай REST API на FastAPI с аутентификацией JWT, CRUD операциями для пользователей и документацией Swagger
```

## ⚙️ Конфигурация

Основные параметры в `config.yaml`:

### Модель

```yaml
model:
  provider: "ollama"  # или "local_transformers"
  model_name: "deepseek-coder:6.7b"
  generation:
    max_tokens: 4096
    temperature: 0.2  # Низкая для детерминированного кода
```

### Контекст проекта

```yaml
agent:
  load_project_context: true  # Автоматически загружать контекст проекта
  project_root: .  # Корневая директория проекта
```

Когда `load_project_context: true`, агент автоматически:
- Загружает структуру проекта
- Читает README файлы
- Анализирует конфигурационные файлы (config.yaml, requirements.txt, package.json и т.д.)
- Находит основные файлы проекта
- Добавляет релевантные файлы в контекст при каждом запросе

Это позволяет моделям **видеть ваш проект** и работать с ним более эффективно!

### GPU настройки

```yaml
gpu:
  use_gpu: true
  max_memory: 24  # Для RTX 3090
  use_flash_attention: true
```

### Системный промпт

Можно настроить поведение агента через `system_prompt` в конфиге.

### Адаптивная система и поддержка любых моделей

Проект автоматически адаптируется под разные модели:

- ✅ **Автоматическое определение возможностей** - Система определяет контекст, параметры и тип модели через API
- ✅ **Поддержка любых моделей** - Работает с любыми моделями через Ollama, LM Studio, OpenAI, Anthropic или кастомные API
- ✅ **Умное определение по имени** - Если API недоступен, система определяет возможности по имени модели
- ✅ **Оптимизация параметров** - Temperature, top_p, max_tokens автоматически настраиваются под модель
- ✅ **Умное управление контекстом** - Контекст проекта автоматически ограничивается под размер модели
- ✅ **Защита от ошибок** - Предотвращает переполнение контекста

**Как это работает:**

1. Система сначала пытается получить информацию о модели через API провайдера (самый точный способ)
2. Если API недоступен, ищет модель в базе данных известных моделей
3. Если модель не найдена, определяет возможности по имени модели (размер, архитектура)
4. Автоматически оптимизирует параметры генерации под возможности модели

Просто укажите модель в `config.yaml` - система сделает всё остальное! Вы можете использовать **любую модель** из любого поддерживаемого провайдера.

## 🎯 Рекомендации для RTX 3090

### Оптимальные модели

1. **deepseek-coder:6.7b** - Лучший баланс скорости и качества
2. **codellama:13b** - Больше параметров, лучше качество
3. **qwen2.5-coder:7b** - Современная архитектура

### Настройки для максимальной производительности

```yaml
gpu:
  use_gpu: true
  max_memory: 24
  use_8bit: false  # Не нужно для 6.7b моделей
  use_flash_attention: true
```

## 📁 Структура проекта

```
AGENT/
├── agent.py          # Основной модуль агента
├── web_ui.py         # Веб-интерфейс (FastAPI)
├── project_context.py # Модуль для работы с контекстом проекта
├── mcp_tools.py      # MCP инструменты для работы с файлами
├── ide_components.py  # Компоненты IDE
├── model_adapter.py   # Адаптер для работы с моделями
├── project_utils.py   # Утилиты для работы с проектом
├── config.yaml       # Конфигурация
├── requirements.txt   # Зависимости
├── templates/         # HTML шаблоны
│   ├── index.html
│   └── ide.html
├── utils/            # Вспомогательные утилиты
└── history/          # История диалогов (создается автоматически)
```

## 🔧 Устранение неполадок

### LM Studio не работает

**Частые проблемы:**

1. **Не удалось подключиться:**
   - Убедитесь, что LM Studio запущен
   - Включите Local Server (Settings → Local Server → Enable Local Server)
   - Проверьте, что порт 1234 не занят

2. **Модель не найдена:**
   - Загрузите модель в LM Studio (Chat → Load Model)
   - Дождитесь статуса "READY"
   - Проверьте точное имя модели в LM Studio
   - Обновите `model_name` в `config.yaml`

3. **Ошибка 502:**
   - Подождите 30-60 секунд после загрузки модели
   - Перезапустите Local Server
   - Используйте меньшую модель

Подробнее: см. `LMSTUDIO_TROUBLESHOOTING.md`

### Ollama не запускается

```bash
# Проверьте статус
ollama serve

# Или запустите вручную
ollama serve
```

### Модель не найдена

```bash
# Список установленных моделей
ollama list

# Скачайте нужную модель
ollama pull deepseek-coder:6.7b
```

### Недостаточно памяти GPU

- Используйте меньшую модель (6.7b вместо 13b)
- Включите квантование в config.yaml:
  ```yaml
  gpu:
    use_8bit: true
  ```

### Медленная генерация

- Убедитесь, что CUDA установлена правильно
- Проверьте, что модель использует GPU:
  ```bash
  nvidia-smi
  ```
- Используйте более легкую модель

## 🚀 Продвинутое использование

### Использование нескольких моделей

Можно переключаться между моделями, изменив `model_name` в config.yaml и перезапустив агента.

### Кастомный системный промпт

Отредактируйте `system_prompt` в config.yaml для изменения поведения агента.

### Сохранение и загрузка сессий

История автоматически сохраняется в `history/`. Сессии сохраняются автоматически при работе через веб-интерфейс.

## 📝 Лицензия

MIT License - используйте свободно!

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📞 Поддержка

Если возникли проблемы:

**Для Ollama:**
1. Проверьте, что Ollama запущен
2. Убедитесь, что модель скачана (`ollama list`)
3. Проверьте конфигурацию в config.yaml

**Для LM Studio:**
1. Убедитесь, что LM Studio запущен и Local Server включен
2. Проверьте, что модель загружена и статус "READY"
3. См. подробное руководство: `LMSTUDIO_TROUBLESHOOTING.md`

**Общее:**
- Проверьте конфигурацию в config.yaml
- Посмотрите логи ошибок в консоли

---

**Наслаждайтесь программированием с AI! 🚀**

