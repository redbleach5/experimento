# 🔧 Решение проблемы 502

## Проблема
Все запросы к LM Studio API возвращают **502 Bad Gateway**, хотя сервер запущен.

## Причина
На скриншоте видно, что включена настройка **"Загрузка модели по требованию"** (Load model on demand). Это означает, что модель загружается только когда нужно, и может быть не загружена для API запросов.

## Решение

### Вариант 1: Отключить "Загрузка модели по требованию"

1. В LM Studio откройте **Settings → Local Server**
2. Найдите **"Загрузка модели по требованию"** (Load model on demand)
3. **Отключите** этот переключатель
4. Перезапустите Local Server (Stop → Start)
5. Дождитесь загрузки модели
6. Попробуйте снова

### Вариант 2: Загрузить модель вручную

1. В LM Studio перейдите в раздел **Chat**
2. Убедитесь, что модель **загружена** (должна быть видна в списке)
3. Если модель не загружена - загрузите ее
4. После загрузки попробуйте API запросы

### Вариант 3: Отправить запрос в Chat сначала

1. Откройте **Chat** в LM Studio
2. Отправьте любое сообщение модели
3. Дождитесь ответа
4. Это "разбудит" модель и загрузит ее в память
5. После этого API запросы должны работать

### Вариант 4: Отключить "Автоматическая разгрузка"

1. В **Settings → Local Server**
2. Найдите **"Автоматическая разгрузка неиспользуемых моделей"**
3. **Отключите** этот переключатель
4. Это предотвратит разгрузку модели

## Рекомендуемые настройки

Для стабильной работы API рекомендуется:

- ✅ **Загрузка модели по требованию**: **ОТКЛЮЧЕНО**
- ✅ **Автоматическая разгрузка**: **ОТКЛЮЧЕНО** (или увеличьте TTL)
- ✅ **Local Server**: **ВКЛЮЧЕНО**
- ✅ **CORS**: **ВКЛЮЧЕНО** (если нужно)

## После настройки

После применения этих настроек:

1. Перезапустите Local Server
2. Дождитесь полной загрузки модели
3. Попробуйте запрос через агента:

```bash
python final_chat_test.py
```

Или используйте GUI:
```bash
python gui.py
```

## Альтернатива: Использовать Ollama

Если проблема сохраняется, можно использовать Ollama:

1. Измените `config.yaml`:
```yaml
model:
  provider: "ollama"
  model_name: "deepseek-coder:6.7b"
```

2. Установите модель:
```bash
ollama pull deepseek-coder:6.7b
```

3. Запустите агента - должно работать сразу

