# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ: ПУШ ПРОЕКТА В GIT

## 📋 ТЕКУЩИЙ СТАТУС

✅ **Локальный репозиторий готов:**
- Git инициализирован
- Все файлы добавлены (98 файлов, 11644 строк)
- Создано 2 коммита
- Remote настроен: `https://github.com/redbleach5/experimento.git`

## 🔍 ЧТО ПРОИСХОДИТ

### Структура проекта:
```
AGENT/
├── agent.py              # Основной агент
├── gui.py                # GUI интерфейс
├── cli.py                # CLI интерфейс
├── web_ui.py             # Веб интерфейс
├── config.yaml           # Конфигурация
├── requirements.txt       # Зависимости
└── ... (98 файлов)
```

### Git статус:
- **Ветка:** main
- **Коммиты:** 2
  - `e0fac70` - Initial commit: AI Agent для работы с локальными моделями
  - `ce27400` - Add Git push instructions
- **Remote:** origin → https://github.com/redbleach5/experimento.git

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

### Шаг 1: Создать репозиторий на GitHub

1. **Зайдите на GitHub:**
   - Откройте: https://github.com/new
   - Или: https://github.com/redbleach5?tab=repositories → New

2. **Заполните форму:**
   - **Repository name:** `experimento`
   - **Description:** `AI Agent для работы с локальными моделями (LM Studio, Ollama) - заебца проект`
   - **Visibility:** Public или Private (на ваш выбор)
   - **ВАЖНО:** НЕ ставьте галочки на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - (У нас уже есть все эти файлы!)

3. **Нажмите:** "Create repository"

### Шаг 2: Аутентификация

GitHub больше не принимает пароли. Нужен **Personal Access Token**:

#### Вариант A: Personal Access Token (рекомендуется)

1. **Создайте токен:**
   - Зайдите: https://github.com/settings/tokens
   - Нажмите: "Generate new token" → "Generate new token (classic)"
   - **Note:** `experimento-push`
   - **Expiration:** На ваш выбор (90 days или No expiration)
   - **Scopes:** Отметьте `repo` (полный доступ к репозиториям)
   - Нажмите: "Generate token"
   - **СОХРАНИТЕ ТОКЕН!** (показывается только один раз)

2. **Используйте токен:**
   - При запросе username: введите `redbleach5`
   - При запросе password: вставьте токен (не пароль!)

#### Вариант B: SSH ключи (альтернатива)

1. **Проверьте SSH ключи:**
   ```bash
   ls ~/.ssh
   ```

2. **Если нет ключа, создайте:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

3. **Добавьте в GitHub:**
   - Скопируйте публичный ключ: `cat ~/.ssh/id_ed25519.pub`
   - Зайдите: https://github.com/settings/keys
   - Нажмите: "New SSH key"
   - Вставьте ключ и сохраните

4. **Измените remote на SSH:**
   ```bash
   git remote set-url origin git@github.com:redbleach5/experimento.git
   ```

### Шаг 3: Пуш кода

После создания репозитория и настройки аутентификации:

```bash
git push -u origin main
```

**Если запросит аутентификацию:**
- Username: `redbleach5`
- Password: вставьте Personal Access Token (не пароль!)

## 🔧 АЛЬТЕРНАТИВНЫЕ СПОСОБЫ

### Способ 1: Через GitHub Desktop
1. Установите GitHub Desktop
2. File → Add Local Repository
3. Выберите папку `K:\AGENT`
4. Publish repository

### Способ 2: Через VS Code
1. Откройте проект в VS Code
2. Source Control (Ctrl+Shift+G)
3. Нажмите "..." → "Publish to GitHub"
4. Следуйте инструкциям

### Способ 3: Через веб-интерфейс GitHub
1. Создайте репозиторий на GitHub
2. Скопируйте команды, которые GitHub покажет
3. Выполните их в терминале

## 📝 ПОЛНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ КОМАНД

Если репозиторий уже создан и аутентификация настроена:

```bash
# Проверка статуса
git status

# Проверка remote
git remote -v

# Если remote не настроен:
git remote add origin https://github.com/redbleach5/experimento.git

# Пуш
git push -u origin main
```

## ✅ ПРОВЕРКА УСПЕХА

После успешного пуша:

1. **Проверьте на GitHub:**
   - Откройте: https://github.com/redbleach5/experimento
   - Должны быть видны все файлы

2. **Проверьте локально:**
   ```bash
   git log --oneline
   git remote -v
   ```

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "Repository not found"
- Репозиторий еще не создан → создайте на GitHub
- Неправильный URL → проверьте имя репозитория
- Нет доступа → проверьте права доступа

### Ошибка: "Authentication failed"
- Используйте Personal Access Token вместо пароля
- Проверьте, что токен имеет права `repo`
- Токен не истек

### Ошибка: "Permission denied"
- Проверьте SSH ключи (если используете SSH)
- Проверьте права доступа к репозиторию

## 🎉 ПОСЛЕ УСПЕШНОГО ПУША

Ваш проект будет доступен по адресу:
**https://github.com/redbleach5/experimento**

Все готово к работе! 🚀

