# ✅ Чеклист для настройки LM Studio API

## 🔍 Что нужно проверить в LM Studio:

### 1. Local Server Settings
- [ ] Откройте **Settings → Local Server**
- [ ] Убедитесь что кнопка **"Start Server"** нажата (должно быть "Server running")
- [ ] Проверьте **порт** - должен быть указан (обычно 1234)
- [ ] **ОТКЛЮЧИТЕ** "Load model on demand" (Загрузка модели по требованию)
- [ ] **ОТКЛЮЧИТЕ** "Automatic unloading of unused models" (Автоматическая разгрузка)

### 2. Модель
- [ ] Модель `qwen3-30b-a3b-instruct-2507` должна быть **загружена**
- [ ] В разделе Chat модель должна отвечать (это уже работает ✅)
- [ ] В Local Server модель должна быть видна как **"READY"**

### 3. Перезапуск
- [ ] Нажмите **"Stop Server"**
- [ ] Подождите **5 секунд**
- [ ] Нажмите **"Start Server"**
- [ ] Дождитесь загрузки модели (статус "READY")

### 4. Проверка логов
- [ ] Откройте **View → Developer Logs**
- [ ] Проверьте на наличие ошибок
- [ ] Ищите сообщения о загрузке модели

### 5. Тест в Chat
- [ ] Отправьте сообщение в **Chat** в LM Studio
- [ ] Убедитесь что модель отвечает
- [ ] Это "разбудит" модель для API

## 🔧 Альтернативные решения:

### Если порт не 1234:
1. Проверьте порт в настройках Local Server
2. Обновите `config.yaml`:
   ```yaml
   lmstudio:
     base_url: "http://localhost:ВАШ_ПОРТ"
   ```

### Если модель не загружается для API:
1. В Chat отправьте любое сообщение модели
2. Дождитесь ответа
3. Это загрузит модель в память для API

### Если ничего не помогает:
1. Перезапустите LM Studio полностью
2. Загрузите модель в Chat
3. Включите Local Server
4. Подождите 30 секунд после запуска
5. Попробуйте снова

