# 🚀 Инструкция для пуша в Git

## ✅ Локальный репозиторий готов!

Коммит создан:
```
e0fac70 Initial commit: AI Agent для работы с локальными моделями (LM Studio, Ollama) - заебца проект
```

98 файлов добавлено, 11644 строк кода!

## 📤 Как запушить в удаленный репозиторий:

### Вариант 1: GitHub

1. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com/new
   - Название: `zaebca` (или любое другое)
   - НЕ добавляйте README, .gitignore или лицензию (у нас уже есть)
   - Нажмите "Create repository"

2. **Добавьте remote и запушьте:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/zaebca.git
   git push -u origin main
   ```

### Вариант 2: GitLab

1. **Создайте репозиторий на GitLab:**
   - Зайдите на https://gitlab.com/projects/new
   - Название: `zaebca`
   - Нажмите "Create project"

2. **Добавьте remote и запушьте:**
   ```bash
   git remote add origin https://gitlab.com/ВАШ_USERNAME/zaebca.git
   git push -u origin main
   ```

### Вариант 3: Другой сервис

Просто замените URL в команде:
```bash
git remote add origin ВАШ_URL_РЕПОЗИТОРИЯ
git push -u origin main
```

## 🔐 Если нужна аутентификация:

- **GitHub**: Используйте Personal Access Token вместо пароля
- **GitLab**: Используйте Personal Access Token
- Или настройте SSH ключи

## 📋 Быстрая команда (после создания репозитория):

```bash
# Замените URL на ваш
git remote add origin https://github.com/ВАШ_USERNAME/zaebca.git
git branch -M main
git push -u origin main
```

## ✅ Проверка:

После пуша проверьте:
```bash
git remote -v
git status
```

Готово! 🎉

