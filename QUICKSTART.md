# 🚀 Быстрый старт

## Шаг 1: Установите Ollama или LM Studio

**Вариант 1: Ollama (рекомендуется для CLI)**
1. Скачайте Ollama: https://ollama.ai/download/windows
2. Установите и запустите Ollama
3. Проверьте установку:
   ```bash
   ollama --version
   ```

**Вариант 2: LM Studio (рекомендуется для GUI)**
1. Скачайте LM Studio: https://lmstudio.ai
2. Установите и запустите LM Studio
3. Включите Local Server в настройках (порт 1234)
4. См. подробную инструкцию: `LM_STUDIO_SETUP.md`

## Шаг 2: Установите модель

Для RTX 3090 рекомендуется:

```bash
ollama pull deepseek-coder:6.7b
```

Это займет несколько минут (около 4-5 GB).

## Шаг 3: Установите зависимости Python

```bash
pip install -r requirements.txt
```

Или используйте скрипт установки:

```bash
python setup.py
```

## Шаг 4: Запустите агента

### Вариант 1: Через скрипт (Windows)
```bash
run.bat
```

### Вариант 2: CLI интерфейс
```bash
python cli.py
```

### Вариант 3: Веб-интерфейс
```bash
python web_ui.py
```
Затем откройте: http://127.0.0.1:8000

## Первый запрос

Попробуйте спросить:
```
Напиши функцию на Python для вычисления факториала с рекурсией и итерацией
```

## Настройка

Отредактируйте `config.yaml` для изменения:
- Модели (model_name)
- Параметров генерации (temperature, max_tokens)
- Системного промпта

## Проблемы?

1. **Ollama не запускается**: Убедитесь, что Ollama запущен (должен быть в трее)
2. **Модель не найдена**: Выполните `ollama pull deepseek-coder:6.7b`
3. **Медленная работа**: Проверьте, что GPU используется (`nvidia-smi`)

---

**Готово! Наслаждайтесь программированием с AI! 🎉**

