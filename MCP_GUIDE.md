# 🔧 MCP (Model Context Protocol) - Руководство

## Что такое MCP?

MCP (Model Context Protocol) - это протокол, который позволяет AI агенту использовать внешние инструменты и получать контекст для более точных ответов.

## ✅ Что реализовано

### Доступные инструменты:

1. **read_file** - Чтение файлов
   - Параметры: `file_path` (str)
   - Пример: `TOOL_CALL: read_file {"file_path": "config.yaml"}`

2. **write_file** - Запись в файлы
   - Параметры: `file_path` (str), `content` (str)
   - Пример: `TOOL_CALL: write_file {"file_path": "test.txt", "content": "Hello"}`

3. **list_files** - Список файлов в директории
   - Параметры: `directory` (str, optional, по умолчанию ".")
   - Пример: `TOOL_CALL: list_files {"directory": "."}`

4. **execute_command** - Выполнение команд в shell
   - Параметры: `command` (str)
   - Пример: `TOOL_CALL: execute_command {"command": "ls -la"}`

5. **web_search** - Поиск в интернете (базовая реализация)
   - Параметры: `query` (str)
   - Пример: `TOOL_CALL: web_search {"query": "Python best practices"}`

## 🚀 Как это работает

### Автоматическое использование

Агент автоматически получает информацию о доступных инструментах в системном промпте. Когда модель понимает, что нужен инструмент, она может вызвать его в формате:

```
TOOL_CALL: tool_name {"param1": "value1", "param2": "value2"}
```

### Процесс работы:

1. Пользователь задает вопрос
2. Агент анализирует запрос
3. Если нужен инструмент - агент вызывает его
4. Инструмент выполняется
5. Результаты возвращаются агенту
6. Агент использует результаты в финальном ответе

### Пример диалога:

**Пользователь:** "Прочитай config.yaml и скажи какая модель используется"

**Агент:**
1. Понимает, что нужно прочитать файл
2. Вызывает: `TOOL_CALL: read_file {"file_path": "config.yaml"}`
3. Получает содержимое файла
4. Анализирует и отвечает: "В config.yaml используется модель qwen3-30b-a3b-instruct-2507"

## ⚙️ Настройка

В `config.yaml`:

```yaml
mcp:
  # Включить ли MCP инструменты
  enabled: true
  
  # Максимальное количество итераций с инструментами
  max_iterations: 5
```

## 🧪 Тестирование

### Тест инструментов:
```bash
python test_mcp.py
```

### Тест агента с MCP:
```bash
python test_agent_with_mcp.py
```

## 📝 Примеры использования

### Пример 1: Чтение файла
```
Пользователь: "Что в файле requirements.txt?"
Агент: Использует read_file и отвечает на основе содержимого
```

### Пример 2: Список файлов
```
Пользователь: "Какие Python файлы есть в проекте?"
Агент: Использует list_files, фильтрует .py файлы и отвечает
```

### Пример 3: Выполнение команды
```
Пользователь: "Проверь версию Python"
Агент: Использует execute_command с "python --version" и отвечает
```

## 🔒 Безопасность

- Команды выполняются с таймаутом 30 секунд
- Ограничение на количество итераций (max_iterations)
- Проверка путей файлов (нельзя выйти за пределы проекта)

## 🎯 Преимущества

1. **Больше контекста** - агент может читать файлы проекта
2. **Выполнение команд** - может проверять код, запускать тесты
3. **Автоматизация** - может создавать и изменять файлы
4. **Точность** - ответы основаны на реальных данных

## 🚀 Будущие улучшения

- Подключение к базам данных
- Работа с Git
- Интеграция с IDE
- Поддержка внешних MCP серверов

